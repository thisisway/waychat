{
  "name": "@waychat/widget",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/check-size.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "preact": "^10.29.8",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "jsdom": "^30.1.1",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
